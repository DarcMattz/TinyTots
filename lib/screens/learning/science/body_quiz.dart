import 'package:flutter/material.dart';

class BodyQuizScreen extends StatelessWidget {
  const BodyQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Quiz"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class CareQuizScreen extends StatelessWidget {
  const CareQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Quiz"),
      ),
    );
  }
}
